{
  "name": "data",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/app/data/src",
  "prefix": "tfn",
  "projectType": "library",
  "tags": ["scope:app", "type:data"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/app/data"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
